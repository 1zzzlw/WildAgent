# WildAgent 项目进展总结

> 最后更新：2026-07-23

## 1. 项目概述

**WildAgent** 是一个 AI 辅助的参数化 3D 建筑编辑器。用户用自然语言描述建筑，系统自动生成 `.wild` 蓝图文件并实时渲染 3D 效果。

### 核心理念

| 原则 | 说明 |
|---|---|
| `.wild` 是唯一场景源文件 | 所有建筑结构、材质、行为最终都落到 `.wild` JSON |
| 渲染引擎只复用不重做 | `wild-core` 是现有核心引擎，新系统只接入它 |
| AI 不直接生成三角形 | AI 只生成参数、构件、材质、patch |
| 用户保留最终决策权 | 批量修改、删除、覆盖场景必须用户确认 |
| 手动编辑不走 Agent | 拖拽、属性面板、场景树操作直接修改本地状态 |

不同于主流 AI 3D 生成工具（Meshy/Tripo）直接生成三角网格，WildAgent 采用：
- **AI 只填参数表**，不生成几何
- **数学引擎负责计算**，保证精确和可复现
- **产物是活参数文件**（几 KB），不是死网格文件（几十 MB）
- **完全可编辑**，每个参数独立可调

### 三大模块

```
┌─────────────────┐
│   wild-core     │  已有：渲染/重建引擎（不重做）
│  (引擎核心)      │  输入 .wild → 输出几何
└─────────────────┘

┌─────────────────┐
│   wild-web      │  新建：前端编辑器
│  (前端编辑器)    │  UI + 状态管理 + Three.js 渲染
└─────────────────┘

┌─────────────────┐
│   wild-server   │  新建：后端 Agent 服务
│  (AI 服务)       │  理解意图 → 生成 .wild / Patch
└─────────────────┘
```

---

## 2. 技术栈

| 层 | 技术 |
|---|---|
| 前端框架 | Vue 3.5 + TypeScript 6.0（组合式 API） |
| 状态管理 | Pinia 2.1 |
| 3D 渲染 | Three.js 0.160 |
| 构建工具 | Vite 8.1 |
| 实时通信 | WebSocket |
| 后端框架 | FastAPI + uvicorn |
| AI Agent | LangChain / LangGraph |
| LLM | OpenAI 兼容接口 |
| 包管理 | uv（Python）/ npm（前端） |

---

## 3. 开发阶段总览

### Phase 1：前端基础框架 ✅ 已完成

| 模块 | 状态 | 说明 |
|---|---|---|
| 状态管理（5 个 Store） | ✅ | sceneStore / selectionStore / historyStore / agentStore / uiStore |
| ScenePatch 协议 | ✅ | 统一编辑操作格式，基于 revision 版本控制 |
| UI 组件框架 | ✅ | 4 布局面板 + 5 功能面板 + 3D 视口 |
| wild-core 引擎内嵌 | ✅ | parser / expander / resolver / 10 种几何构建器 + 材质系统 |
| Three.js 渲染适配层 | ✅ | wildCoreAdapter → meshDataToGeometry → materialAdapter → renderEntity |
| 3D 视口 | ✅ | 灯光 / 阴影 / 轨道控制 / 响应式布局 |
| 基础编辑功能 | ✅ | 拖拽添加构件 / 属性面板 / 场景树 / .wild 导入导出 |

### Phase 2：后端 Agent MVP 🚧 接近完成

| 模块 | 状态 | 说明 |
|---|---|---|
| FastAPI + WebSocket 端点 | ✅ | 心跳 ping/pong / 消息分发 / 并发锁 / 思考动画 |
| System Prompt 组装 | ✅ | 统一 prompt（生成 + 修改 + 聊天三种意图，AI 自行判断） |
| 规范文档加载器 | ✅ | FileSpecLoader，预留 RAG 接口 |
| 16 个空间工具 | ✅ | 1 查询 + 9 检测 + 6 修正，生成/修改通用 |
| 15 步校验流水线 | ✅ | Structure → Schema → Reference → Geometry → Fix → Collision |
| LLM 模型客户端 | ✅ | OpenAI 兼容工厂 |
| LangChain Agent | ✅ | Agent 创建 + 工具绑定 |
| Blueprint 提取/校验/保存 | ✅ | 支持 Blueprint + ScenePatch 双格式自动识别 |
| 前后端 WebSocket 联通 | ✅ | 心跳 + 自动重连 + 页面可见性检测 |
| 端到端对话测试 | ✅ | AI 对话生成蓝图全链路跑通 |
| 增量修改（AI Patch） | ✅ | AI 识别修改意图 → ScenePatch → 前端"应用修改" → 累加到同一蓝图 → 后端 PUT 同步 |
| Patch 校验拦截 | ✅ | 校验失败的 patch 不发送给前端，改为错误提示 |
| 会话管理 | ✅ | 会话列表（Pinia + localStorage）+ 刷新恢复 + 蓝图文件按 session_id 命名 |
| RAG 索引向量库 | 🚧 | 提高 AI 建模精度（**下一优先级**） |

### Phase 3：完整编辑工作流 ⏳ 待开始

- 3D 点击拾取和选中高亮
- Transform Gizmo（平移/旋转/缩放）
- Agent Patch 确认 UI（建议 → 预览 → 应用/拒绝）
- 自动保存
- 更多 Agent 工具

### Phase 4：稳定化和扩展 ⏳ 待开始

- 后端场景持久化
- 错误恢复
- 性能优化（局部重建等）
- 更多构件类型支持

---

## 4. 核心架构设计

### 4.1 Blueprint（蓝图）

`.wild` 文件的 JSON 格式，是场景的唯一源文件。包含：
- `meta`：版本/类型/名称
- `geometry`：构件数组（elements）
- `materials`：材质定义
- `behaviors`：行为脚本

### 4.2 ScenePatch（场景补丁）

所有修改操作的统一格式，用户操作和 Agent 建议都转为 ScenePatch：

```typescript
interface ScenePatch {
  type: 'scene_patch'
  patch_id: string
  base_revision: number        // 基于哪个版本，不匹配则拒绝
  source: 'user' | 'agent' | 'system'
  mode: 'apply' | 'proposal'
  requires_confirmation: boolean
  operations: SceneOperation[]  // add/update/remove/upsert
  summary?: string
}
```

- 每次成功应用后 `revision++`
- Patch 的 `base_revision` 必须等于当前 revision，防止冲突

### 4.3 数据流

```
用户操作 / Agent 建议 → ScenePatch → sceneStore.applyPatch()
  → Blueprint 更新 → 校验
  → wild-core.reconstructEntity()
  → MeshData + MaterialParams → Three.js 渲染
```

---

## 5. 前端关键模块

### 状态管理（5 个 Pinia Store）

| Store | 职责 |
|---|---|
| `sceneStore` | 场景文档 + Blueprint + 重建触发 |
| `selectionStore` | 选中 / hover 状态 |
| `historyStore` | 撤销/重做（50 步历史） |
| `agentStore` | AI 对话消息 + 会话列表管理 + WebSocket 连接状态 |
| `uiStore` | UI 面板布局状态 |

### 渲染管线

```
wildCoreAdapter (parse + reconstruct)
  → meshDataToGeometry (MeshData → BufferGeometry)
  → materialAdapter (MaterialParams → Three.js 材质 + 缓存)
  → renderEntity (ReconstructedEntity → Three.js Group)
  → CanvasViewport (灯光 + 阴影 + 轨道控制 + 响应式)
```

### wild-core 几何构建器（10 种）

wall / floor / column / beam / roof / stair / opening / furniture / body / denseBrick

### Agent 通信

- `agentBridge.ts`：WebSocket 桥接（心跳 + 自动重连 + 指数退避 + 页面可见性检测）+ HTTP 蓝图同步/加载
- `AIChatPanel.vue`：AI 对话 UI（消息列表 + 输入框 + 思考动画 + 会话切换栏）

### 会话管理

- 一个会话 = 一个蓝图文件 `{session_id}.wild`，一一对应
- 会话列表存储在 Pinia + localStorage（刷新不丢失）
- 增量修改后自动通过 HTTP PUT 同步到后端文件
- 页面刷新自动恢复到上次会话的蓝图
- 支持多会话切换，每个会话对应一个独立建筑

---

## 6. 后端关键模块

### 目录结构

```
wild-server/
├── main.py                    # FastAPI 入口（uvicorn）
├── config.py                  # Pydantic Settings 配置
├── storage/
│   ├── knowledge_base/        # 规范文档
│   ├── scenes/                # 生成的 .wild 文件
│   └── sessions/              # 会话数据
└── app/
    ├── api/
    │   ├── ws_agent.py        # WebSocket 端点
    │   └── scenes.py          # 场景 HTTP API
    ├── services/
    │   ├── agent_service.py   # Agent 服务（15 步校验流水线）
    │   └── session_service.py
    ├── agent/
    │   ├── graph.py           # LangGraph Agent 图（预留）
    │   ├── prompts.py         # System Prompt 组装
    │   └── model_client.py    # LLM 模型客户端
    ├── tools/
    │   └── spatial_tools.py   # 16 个空间工具
    ├── spec/
    │   └── loader.py          # 规范文档加载器
    └── utils/
        ├── blueprint_parser.py
        └── ws_heartbeat.py
```

### Scenes REST API

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/scenes` | 列出所有已保存的蓝图文件 |
| GET | `/api/scenes/{filename}` | 获取单个蓝图文件 |
| PUT | `/api/scenes/{filename}` | 更新/保存蓝图文件 |

### 15 步校验流水线

Structure → Schema → Reference → Geometry → Fix → Collision

### 空间工具（16 个）

| 类别 | 数量 | 示例 |
|---|---|---|
| 查询 | 1 | get_wall_bounding_box |
| 检测 | 9 | validate_* 系列（高度/位置/碰撞/连接等） |
| 修正 | 6 | fix_* 系列（自动修正检测到的问题） |

---

## 7. 近期提交历史

| 提交 | 说明 |
|---|---|
| `5e90879` | AI 对话功能完成，后续增加 RAG 索引向量库，提高 AI 建模的精确度 |
| `d8245fb` | 优化渲染引擎代码，修复渲染建模 bug，编写 AI 生成蓝图规范文档 |
| `b1ed4ea` | 完成前端项目渲染、架构搭建 |
| `3a3875e` | README 文档初始化 |
| `bf0acdd` | 初版架构设计方案 |

### 当前工作区未提交改动

**后端**：
- `agent_service.py` — 15 步校验流水线 + 统一 query_structured()（Blueprint/ScenePatch 双格式）
- `spatial_tools.py` — 16 个空间工具（1 查询 + 9 检测 + 6 修正）
- `prompts.py` — 统一 System Prompt（生成/修改/聊天三种意图，AI 自行判断）
- `ws_agent.py` — 增量修改路由 + session_id 文件命名
- `scenes.py` — 新增 PUT + LIST 端点
- `blueprint_parser.py` — 新增 save_blueprint_file_as + extract_patch_from_text

**前端**：
- `agentBridge.ts` — patch_proposal 消息处理 + HTTP 蓝图同步/加载方法
- `agentStore.ts` — 会话列表管理 + localStorage 持久化
- `AIChatPanel.vue` — 会话切换栏 + 增量修改应用 + 刷新恢复
- `types/agent.ts` — 新增 SessionInfo 类型 + UserMessageRequest 加 blueprint 字段
- `protocol.ts` — 传递 blueprint 到后端

---

## 8. 当前优先级

1. **RAG 索引向量库** — 当前最紧迫任务，提升 AI 生成蓝图的精度
2. **3D 交互增强** — 点击拾取、选中高亮
3. **Transform Gizmo** — 平移/旋转/缩放
4. **消息历史恢复** — 刷新后恢复聊天记录（目前只恢复场景蓝图）
5. **自动保存** — 用户手动编辑时自动同步到后端

### 架构备忘：wild-core 放置位置

wild-core（22 个 TS 文件）当前放在 `wild-web/src/wild-core/`。**现阶段合理，Phase 3 以后计划抽为独立 package（`@wild/core`）**。原因：
- 引擎不依赖任何前端框架，是独立的核心资产
- 目前改动频繁，放前端内调试方便；稳定后抽走更划算
- 抽取后可以有独立版本号和单元测试，wild-server 将来也能引用做服务端校验

详见 [架构设计方案.md §17.5](架构设计方案.md)。

---

## 9. 关键文件索引

### 文档

| 文件 | 说明 |
|---|---|
| [架构设计方案.md](架构设计方案.md) | 完整架构设计（最重要） |
| [项目进展总结.md](项目进展总结.md) | 本文档 |
| [wild-web/CLAUDE.md](wild-web/CLAUDE.md) | 项目快速了解文档 |
| [wild-web/README.md](wild-web/README.md) | 前端架构说明（含 ScenePatch 协议详解） |
| [wild-web/docs/FRONTEND_API.md](wild-web/docs/FRONTEND_API.md) | 前后端接口文档 |

### 前端核心

| 文件 | 说明 |
|---|---|
| [sceneStore.ts](wild-web/src/stores/sceneStore.ts) | 场景状态管理 |
| [scenePatch.ts](wild-web/src/wild/scenePatch.ts) | Patch 应用逻辑 |
| [wildCoreAdapter.ts](wild-web/src/renderer/wildCoreAdapter.ts) | wild-core 封装入口 |
| [renderEntity.ts](wild-web/src/renderer/renderEntity.ts) | 场景 → Three.js 转换 |
| [CanvasViewport.vue](wild-web/src/components/viewport/CanvasViewport.vue) | 3D 视口 |
| [agentBridge.ts](wild-web/src/agent/agentBridge.ts) | Agent WebSocket 通信 |

### 后端核心

| 文件 | 说明 |
|---|---|
| [ws_agent.py](wild-server/app/api/ws_agent.py) | WebSocket 端点 |
| [agent_service.py](wild-server/app/services/agent_service.py) | Agent 服务核心 |
| [spatial_tools.py](wild-server/app/tools/spatial_tools.py) | 16 个空间工具 |
| [prompts.py](wild-server/app/agent/prompts.py) | System Prompt 组装 |
| [loader.py](wild-server/app/spec/loader.py) | 规范文档加载器 |
| [blueprint_parser.py](wild-server/app/utils/blueprint_parser.py) | Blueprint 提取/校验 |
