## RAG

### 知识库文档

#### 编写规范
1. 了解生产级的RAG系统
它需要在检索质量、系统性能、安全合规、可观测性等多个维度达到企业级标准。其核心目标可以概括为三个支柱：高准确率（Accuracy）、高性能（Performance）和合理的总体拥有成本（TCO）

2. 富元数据驱动模式：开发者往往直接把 Markdown 文件丢进去，只保留正文，没有任何文件头。这样向量库里的每一条记录就只有两列：id 和 text（贫元数据驱动模式的写法）。
    ```
        ---
        doc_type: component
        doc_scope: generation
        knowledge_layer: wild_schema
        entity_type: window
        entity_name: static_window_component
        topic: schema
        wild_version: "1.1"
        status: supported
        authority: engine
        source: wild-web/wild-lang/schema.json
        keywords:
            - 基础静态窗
            - window component
            - geometry.components
            - parentWall
            - verticalMullions
            - glassMaterial
        ---
    ```
以上的写法就是带 Frontmatter 的规范写法，具备生产级 RAG 必须的特征
* 实体抽象（Entity Abstraction）：定义了 entity_type（窗口）和 entity_name（具体组件），这允许上层应用做权限隔离（比如普通用户只能搜 supported 状态，管理员能看 deprecated）。
* 生命周期管理（Lifecycle）：status: supported 和 wild_version 让系统能优雅地处理知识过期（比如升级到 2.0 版本后，老版本知识还能保留给特定旧项目用）。
* 去重与溯源（Provenance）：source 字段精确到 JSON 文件，这是实现可解释 AI（XAI）的基础，用户问“你凭什么这么说”，系统能直接甩出文件路径。

3. RAG原型与企业生产级能力差距对比：对比评估要同时看问答质量、引用溯源、敏感数据控制、项目生命周期四个维度，不能只看回答是否通顺。 

    | 评估维度     | 原型演示 RAG                                             | 企业生产级 RAG 目标                                                |
    | ------------ | -------------------------------------------------------- | ------------------------------------------------------------------ |
    | 问答质量     | 侧重回答通顺；测试样本偏理想，缺少冲突、过期、模糊类问题 | 兼顾召回、准确、拒答能力；可处理业务模糊、信息缺失、多版本冲突场景 |
    | 引用溯源     | 简单展示文档名；无法定位片段；幻觉输出无标记             | 精确片段引用，可跳转原始文档；区分知识库输出与模型固有知识         |
    | 敏感数据控制 | 不做权限过滤；所有人获取相同召回结果，存在越权风险       | 召回阶段执行权限过滤；完整留存问答、访问、溯源审计日志             |
    | 项目生命周期 | 一次性导入文档；文档更新后向量库不会自动失效             | 支持增量更新、过期作废；评测流程可重复执行，支持版本回归测试       |

4. RAG评测风险‑控制指标框架：完整评测分为四大模块：召回指标、答案准确性指标、引用质量指标、业务成功率指标，同时叠加安全校验项，识别各类风险点。

    | 风险环节     | 传统评测缺口                                           | 建议评测指标                                                       | 业务安全价值                                                   |
    | ------------ | ------------------------------------------------------ | ------------------------------------------------------------------ | -------------------------------------------------------------- |
    | 召回质量     | 只看最终回答，不核查底层召回片段；忽略漏召、误召       | 召回准确率、召回覆盖率；漏召率、误召率；多版本文档召回表现         | 保证问题对应的关键业务材料能够被检索到，减少答案缺少依据的情况 |
    | 答案准确性   | 主观判断通顺度；不区分幻觉、事实错误、信息缺失         | 事实正确率；幻觉发生率；信息缺失时拒答能力；冲突信息处理能力       | 降低业务场景输出错误信息带来的决策风险                         |
    | 引用质量     | 仅展示文档名称；引用与回答内容不做校验；虚假引用不识别 | 引用匹配度；引用可跳转；虚假引用占比；区分知识库知识与模型固有知识 | 支持业务人员核验答案依据，满足复盘、审计的可追溯要求           |
    | 业务成功率   | 使用通用测试问题，不贴合真实岗位业务问题               | 业务问题解决率；用户确认采纳率；人工复核通过率；无效问答占比       | 衡量 RAG 是否真正减轻员工业务负担，而不是只完成技术演示        |
    | 安全合规校验 | 评测环节完全忽略权限、审计、越权场景                   | 越权召回测试；敏感信息泄露测试；问答溯源审计日志完整性             | 守住企业数据安全底线，防止 RAG 带来数据泄露风险                |

5. 完整 RAG 系统设计链路

    阶段一：离线数据工程（文档切片 → 向量化）
    此阶段运行一次或源文件变更时触发，产出向量数据库。
    * 输入：原始文件（如你的 windows-supported.md）。
    * 处理流程：
        1. 元数据提取：解析文件头 YAML（doc_type, entity_name, keywords 等）和 Markdown 结构（heading_path 标题层级）。
        2. 智能切片：按 chunk_size=900, overlap=150 切割，强制在 ## 标题处断开，确保表格和代码块完整。
        3. 元数据继承：将提取的 YAML 属性、文件路径、标题层级、时间戳、以及命令行传入的 namespace，全部注入到每一个分片的 metadata 字段中。
        4. 向量化入库：将分片正文（content）送入 Embedding 模型转为向量，连同完整的 metadata 一并存入向量数据库（如 Milvus/Chroma）。
    * 产出：向量数据库中的 [向量值 + 正文 + 完整元数据] 条目。
    
    阶段二：在线查询推理（检索 → 生成）
    此阶段每次用户提问时实时执行。

    * 输入：用户问题 + 用户身份信息（如 user_id, department）。
    * 处理流程：
        1. 权限预过滤（Pre-filter）：根据用户身份，构造过滤条件（如 namespace=test, department=engineering, status=supported），确保后续检索只在权限范围内进行。
        2. 向量检索：将用户问题转为向量，在数据库中检索 Top-K（如 K=5）个最相似的 chunk_id，返回正文和相似度分数。
        3. 拒答门控（Retrieval Gate）：检查最高相似度分数。若低于阈值（如 < 0.45），直接返回“知识库中暂无相关信息”，终止流程，不进入生成阶段。
        4. 构造上下文：将检索到的 K 个分片的正文拼接成 Prompt 的“参考资料（Context）”部分。
        5. LLM 生成：将“参考资料 + 用户问题 + 系统指令”发给大模型，获取最终回答。可要求模型输出结构化 JSON（含 answer, confidence, cited_chunk_ids）。
    * 产出：最终回答文本 + 引用来源 ID。

    阶段三：横切支撑能力（贯穿全链路的“底座”）
    这些不是链路中的某一步，而是必须嵌入到阶段一和阶段二中的“基础设施”。

    1. 全链路可观测性（解决“怎么调优”问题）

        * 为每一次请求生成唯一 request_id，记录：用户问题、检索到的 Chunk IDs 及分数、LLM 输入 Token 数、输出 Token 数、各阶段耗时（Latency）、最终回答、用户反馈（👍/👎）。

        * 关键指标：空召回率（拒答比例）、平均相似度、P95 延迟、Token 成本。

    2. 敏感数据与权限控制（解决“哪些能看”问题）

        * 检索前过滤：利用阶段一注入的 metadata（如 namespace, clearance_level），在向量检索的 filter 参数中强制限定数据范围。

        * 输入脱敏：用户问题进入系统前，使用 PII 检测工具（如 Presidio）对手机号、身份证号等进行脱敏处理。

    3. 质量评估体系（解决“好不好”问题）

        * 离线评估（Golden Set）：维护至少 50-100 条“问题-标准答案-应召回文档 ID”的测试集。每次修改切片策略或 Embedding 模型后，离线跑分，确保 Context Recall@K（召回覆盖率）和 Faithfulness（忠实度）不下降。

        * 在线监控：利用阶段三的日志，定期计算 Answer Relevance（可用 LLM-as-Judge 自动打分）。

    4. 可配置的拒答机制（解决“乱说话”问题）

        * 硬阈值拒答：阶段二中的相似度阈值。

        * 软阈值拒答：LLM 输出中自带的 confidence（置信度）低于 0.6 时，将回答替换为“无法确定”。

        * 兜底拒答：检测到涉政、涉黄等安全红线词汇时，直接拒绝。

