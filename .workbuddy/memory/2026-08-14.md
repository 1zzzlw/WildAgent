# 2026-08-14

## 蓝图生成 + 渲染引擎接入（viewer）

- 依据 `docs/WILD_BLUEPRINT_SPEC.md`（WILD 1.1）手写了现代双层别墅蓝图：
  `wild-web/lantu/generated_modern_villa.wild`（floor/wall/roof/stair 基础元素 + door/window/balcony 组合构件）。
- 校验脚本 `wild-web/scripts/validate-generated.mjs`：用 wild-compiler + wild-core 执行
  `compileBlueprintComponents` + `reconstructEntity`，检查 diagnostics 错误。结果：0 错误 / 0 警告 / 112 网格。
- 渲染查看器：`wild-web/lantu/viewer/`（独立 Vite 应用），alias 直接引入项目的
  `wild-core`、`wild-compiler` 与 `src/renderer/meshDataToGeometry`，用 Three.js 渲染
  `reconstructEntity` 输出，提供蓝图下拉切换 + 本地文件加载 + 自动旋转/线框。
- 启动方式：`cd wild-web/lantu/viewer && npx vite --port 5180`（已设 `optimizeDeps.disabled=true`，
  避免环境对 `.vite/deps` 缓存清理的安全删除门禁；three 以原生 ESM 提供）。
- 注意：`/bp/<file>` 中间件从 `lantu/` 目录直接读取 .wild；不要改成 `/<file>`（会被 Vite SPA fallback 返回 HTML）。
