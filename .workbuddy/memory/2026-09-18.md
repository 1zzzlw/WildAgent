# 2026-09-18 工作日志

## 祈年殿蓝图生成 + 三道门禁全绿

- 按 `传统建筑-祈年殿蓝图书写规则及蓝图示范.md`（v1.0）用 `gen_qiniandian.py` 生成
  `wild-web/lantu/qiniandian.wild`（141 基础 + 12 组件，v1.1），严格照 §4.8 写 dome
  （span=depth=12, position [30,16,30]=eave_l3 顶）。
- 修门禁①残留两项：
  - door 组件缺 `interaction` → 生成器给 4 door 补 `"interaction":{"mode":"swing",
    "hingeSide":"right","openAngle":90}`（注意 `validate_element_required_fields` 里
    door 必填含 interaction，但 schema.json 的 required 数组不含——两者不一致，按校验器为准）。
  - `validate_roof_top_coverage`(7e) 仍报 12 墙裸露 → 新增 `_has_primitive_cover_above`，
    识别水平 primitive cylinder/box（檐盘/彩画带）横跨墙顶标高即算"盖住"。墙体闭合良好（靠
    eave_l1 r13 / band_l1 r10.56 压顶）。
- 门禁② `audit_roof_top_coverage.py` 同步扩展：新增 `primitive_cover_circles` + 圆形覆盖判定，
  认檐盘 cylinder 为覆盖圆（之前只认 roof footprint → 误报 100% 裸露）。
- 三道门禁结果：① `verify_wild_blueprint.py` → 12/12 ✅（PASS）；② `audit_roof_top_coverage.py`
  → exit 0 PASS；③ `check_blueprint_render.mjs` → 0 error / 1 warning：`roof_dome` 重建范围未
  完全覆盖墙体范围（引擎 `reconstructionDiagnostics.ts:222` 把 dome footprint 与"全部墙包络"比，
  祈年殿顶 dome 只盖最内 cylinder body_l3 r5.5，本就不该盖外墙环 r10.5——设计使然，只标不阻断）。
- 交付物：注册 `祈年殿 · qiniandian` 进 `viewer/main.ts` BLUEPRINTS；并 build 自包含单文件
  `wild-web/lantu/viewer/qiniandian.html`（vite.config.qiniandian.mjs → 内联 JS，743KB，双击即渲染）。

## 引擎/校验器新增常识（下次生成圆攒尖/重檐传统建筑直接套用）
- 传统殿阁用叠级 primitive cylinder 表达台基/殿身/檐盘，dome 屋顶承托在"最上檐盘顶"而非墙顶；
  校验器侧：`get_roof_support_bounds`(已含 primitive 承托) + `validate_roof_top_coverage`(7e) +
  `audit_roof_top_coverage.py` 三处都要认"水平 cylinder = 覆盖/承托"，否则整圈墙误报裸露。
- 墙顶标高对齐：dome position[1] 必须等于最上檐盘顶（eave_l3 顶）；本例 16.0。

## 定位"业务验收未通过：等待对应阶段执行"（edit 意图假失败）
- 现象：`session_1789689987790` 用户输入"生成一个玻璃幕墙"（空场景）→ 分类器判 `edit` →
  patch 节点报 "业务验收未通过：等待对应阶段执行"。
- 三层根因（已用 session 落盘 + 代码确认）：
  1. ⚪ `nodes/classifier_node.py:29` 用 `bool(state["current_blueprint"])` 当 has_current_scene；
     空蓝图(0 elements)仍是 truthy dict → 判成"有场景"，`routing.py:211` 的
     `edit && !has_scene → chat` 兜底不触发。
  2. 🔴 `requirements.py:501` `architecture_dimensions` 分支硬编码
     `consumers=["architecture","skeleton","final_validate"]`（generate 阶段），edit 链路只有
     patch → 该要求在 patch 阶段被 `evaluate_acceptance_results`(`:978`)跳过，停在 pending。
  3. 🟡 `requirements.py:1053` `blocking_acceptance_failures` 把 severity=error 且非
     passed/not_applicable 的都算阻断，`pending`("等待对应阶段执行"，`:679` 占位文案)被误算 →
     `workflow.py:510` 拼出错误文案。
- 落盘证据：session 的 acceptance_results 里 `acc_task_1_1` status=pending、message="等待对应阶段执行"，
  其余 `acc_task_1_2/1_3`(consumers=["patch"]) 都 passed。
- 修复方向：① classifier 判 scene 用 `bool(elements or components)` 而非 `bool(bp)`；②
  `architecture_dimensions`/`architecture_roof_type`/`element_all` 在 phase=patch 时 consumers 收成
  `["patch"]`；③ `blocking_acceptance_failures` 只认 `failed`（pending/unsupported/needs_review 不阻断）。
  待用户确认后落地。

## 已落地修复（"业务验收未通过"三层根因）
- ⚪ `routing.py` 新增 `has_scene_content(blueprint)`：按 `geometry.elements/components` 判空，而非
  `bool(blueprint)`；`classifier_node.py:29`、`ws_agent.py:1653` 两处调用点改用它。
- 🔴 `requirements.py::evaluate_acceptance_results`：`phase=="patch"` 且 consumers 不含 patch（generate 语义）
  时，直接标 `status="not_applicable"`（"该验收属于生成链路阶段，编辑链路不适用"），不再停在 pending。
  ——注意：**没改** `_compile_acceptance` 的量化分支（避免改 validator/consumers 引入 failed），降级放在 evaluate 层。
- 🟡 `requirements.py::blocking_acceptance_failures`：只认 `status=="failed"` 阻断；pending/unsupported/needs_review 不阻断。
- 回归：`tests/agent/test_execution_plan.py` 新增 `test_edit_plan_generate_semantics_marked_not_applicable_not_blocking`；
  并把 `test_dynamic_task_completes_only_after_each_acceptance_passes` 旧断言（pending 算阻断）改为
  `assert not blocking_acceptance_failures(...)`。
- 验证：graph 编译 OK、静态门禁 0 问题、test_plan_mode_acceptance 4✓、test_execution_plan 21✓（剩 1 条预存
  层数约束失败，与本次无关）、test_agent_graph_routing 29✓、test_material_kind_requirement 16✓。
  内联复现脚本确认 edit 链路 generate 语义 → not_applicable、blocking 空。

## 修复幕墙建筑 "door 立面槽位数量 1 不在配额 2~2 内"
- 现象：`session_1789689987790` turn2 "生成一个玻璃幕墙" 判 `generate` → architecture 节点
  `DesignDocument` 契约抛 "door 立面槽位数量 1 不在配额 2~2 内"（模型输出 door 2~2，立面实际 1 门槽）。
- 根因：`planning.py::normalize_architecture_plan` 的槽位重派生守卫原为
  `if not curtain_wall and complexity!="minimal" and rep_mode=="full"`，幕墙建筑被整体跳过 →
  door 配额不被重派生成立面槽位数，模型 door 2~2 存活 → `contracts.py:273` 判 1 不在 [2,2]。
- 修复：`planning.py` 重派生守卫改为 `if complexity!="minimal" and rep_mode=="full"`，循环内对
  `curtain_wall and opening_type=="window"` 单独 `continue`（窗保持密铺宽区间 1~480），door 恢复
  逐层 pattern 一一对应。幕墙 front ground_pattern 固定 1 door → door 配额被钉成 1~1。
- 回归：`tests/agent/test_architecture_quota_consistency.py` 新增
  `test_curtain_wall_door_quota_matches_facade_slots`（"生成一个玻璃幕墙"/"…办公楼" 两消息）：
  door min==max==slots，window 保持 1~480 且 max>slots，DesignDocument 可构造。
- 验证：配额一致性 16✓+1 xfail；架构相关 29✓+1 xfail；静态门禁 0 问题；graph 编译 OK；
  内联复现（raw door=2~2）→ 归一化后 door 1~1、DesignDocument OK。
- 备注：schematic 幕墙高层（rep_mode=schematic）仍走宽区间 door 1~4（与非幕墙 schematic 一致），
  槽位 1 落在 [1,4] 内通过；模型若在 schematic 下硬给 tight door 配额仍是理论风险，但与本次 full
  模式 bug 无关，未动。

## 动态节点架构大改造 —— 思路梳理 + 规划文档
- 与用户多轮梳理，确认核心重构方向：**意图分类 → 方案设计（设计节点）→ 动态执行调度器 → 最终校验**。
- 关键决策（已达成一致，写进规划文档）：
  1. 设计图纸 = 结构化 schema，**双投影**：JSON 喂模型、SVG 喂用户；模型从不看图，只读写 JSON。
     schema 分两层：房间拓扑层 + 构件槽位层（用户明确"分层"）。
  2. 调度器 = 黑板/缺口循环：读 missing → 动态创建生成节点(拉构件知识) → 生成 → 合并节点(拉规则) →
     增量 patch 进蓝图 → 确定性对账回写 filled/missing。
  3. 缺口两种：计划内缺口（图纸 missing）+ 涌现缺口（合并时暴露，如楼梯→楼板开口），后者必须回写。
  4. filled/missing 由程序确定性 diff 决定，LLM 不许自报完成。
  5. 节点来自白名单（NODE_REGISTRY），模型只能选节点+填参数，不能发明节点。
  6. 红线：能力缺失只标记不阻断；发散≠什么都支持（"先外墙后内墙"=调度粒度放开，"先做地基"=能力边界标 unsupported）。
  7. plan/dynamic_tasks 编译链废弃，图纸取代任务列表，plan_review 审 SVG 图。
- 产出：`docs/面试难点解决过程/Agent工作流与中间状态设计问题/动态节点设计规划.md`
  （含：失效现状清理清单 §2（🔴删/🟡删留算子/⚪改造三级）、schema 设计 §3、节点库 §4、调度器 §5、
  知识按需拉取 §6、复用映射 §8、分阶段实施 §9（绞杀者模式 P0-P5）、红线 §10）。
- 清理清单要点（供后续改造对照）：planning 调度层(steps/planner/plan_review/planning_research)、
  dynamic_tasks→structured_requirements→acceptance_results 编译链(_compile_acceptance 关键词 if-else、
  phase 白名单、execution_plan_phase_guidance)、固定节点(architecture/material_plan/skeleton/merge/
  组件 gen→val) 全删；但**确定性算子必须抽出下沉**（normalize_architecture_plan、material resolve/apply、
  skeleton 几何展开、assembly.py 修复、12 件套校验器、patch 链路、severity 分级）。
- 后续：用户将据此文档对项目做一次大改造。
