# 2026-08-06 工作记录

## 代码 vs 设计一致性审计（bug 检测）
- 审计了 wild-server / wild-web，对照架构设计方案 / 项目进展总结 / agent.md。
- 主链路接通；关键缺口：B1 完整 Blueprint 致命 ❌ 未拦截下发；F1 remove_element 不级联清理引用；F2 重建失败被吞不 gate 提交。报告：.workbuddy/code-review-2026-08-06.md
- 文档矛盾：RAG 实际已默认实现（Chroma+真实 embedding），但项目进展总结写"未做"，需更新。

## AI 对话框（AIChatPanel）问题
- 用户报两个 bug：发消息后对话框上移+下方空白+对话区变小；LangGraph 多节点流式思考未达预期。
- 诊断：布局因精密思考面板 `flex-shrink:0`(40vh) 插在消息与输入之间 + 消息顶部对齐造成空白；流式思考管线已端到端接好，唯一脆弱点是模型是否回流 `reasoning_content`（`model_client.py` 读 `delta["reasoning_content"]`）。

## 五项需求实现（前端折叠节点 + 后端合并循环 + LangGraph质量 + Token显示 + 拓扑总结）

### 1. 前端：步骤节点独立折叠（kimi-k3）
- `AIChatPanel.vue`：引入 `collapsedNodes: ref<Set<string>>`，每个 `.thinking-node-bar` 点击切换折叠
- 添加 chevron 图标（ArrowRight/ArrowDown），折叠时隐藏 `.reasoning-block` 和 `.diag-row`
- 添加 `node-collapse` 过渡动画 CSS
- vue-tsc + vite build 通过

### 2. 后端：merge_node 校验+修复+循环（deepseek-pro）
- 重写 `merge_node.py`：合并后执行 `run_validation_pipeline` → `_apply_fixes` → 循环（最多 3 轮）
- 每轮通过 `on_reasoning_delta("merge", ...)` 发射思考内容（分片摘要、校验结果、修复状态）
- 新增 `merge_diag` 诊断字段（iterations 数组含每轮 passed/warnings/errors）
- `graph_state.py` 添加 `merge_diag` 字段
- `ws_agent.py` 更新 merge 节点输出处理（含迭代摘要 + debug 消息）

### 3. 后端：LangGraph 生成质量修复（deepseek-pro）
- `component_registry.py`：door/window/railing 的 `extra_rules` 增加数量和位置约束
  - door: 1~2个门，不要每面墙都放
  - window: 每面墙1~3个，按墙长决定
  - railing: 只在有高差的地方放，不在地面层外墙放
- `base_component_node.py`：`_build_user_message` 从"为每面墙生成"改为"合适数量"
- `base_component_node.py`：RAG 查询从 1 条扩展到 2+ 条（增加构件位置规则查询）
- `prompts.py`：`build_component_prompt` 通用规则增加数量克制和位置合理约束

### 4. Token 消耗显示修复
- `model_client.py`：`ReasoningChatOpenAI._convert_chunk_to_generation_chunk` 增加流式 `usage` 捕获
- `model_client.py`：`create_llm` 添加 `stream_usage=streaming` 参数
- `skeleton_node.py`：流式路径增加 `response_metadata.usage` 和 `usage_metadata` 捕获
- `base_component_node.py`：同上，流式路径增加 token 捕获

### 5. LangGraph 拓扑总结
- 编写 `docs/技术解决总结/LangGraph图拓扑总结.md`：完整图拓扑、节点详解、条件路由、状态流转、并发限流、流式思考、Token追踪、与LangChain对比

## 新增意图分类节点 (classifier + chat)

### 新增文件
- `app/agent/nodes/classifier_node.py`: 轻量 LLM 判断 GENERATE/CHAT，降级为关键词匹配
- `app/agent/nodes/chat_node.py`: 全面 RAG 扫描（4条查询）+ LLM 流式生成专业回答

### 修改文件
- `app/agent/graph.py`: 添加 classifier/chat 节点，入口改为 classifier，新增 `_classifier_dispatch` 路由
- `app/agent/graph_state.py`: 添加 intent/chat_reply/chat_diag 字段
- `app/agent/nodes/__init__.py`: 导出 classifier_node 和 chat_node
- `app/api/ws_agent.py`: 添加 classifier/chat 标签、节点事件处理、chat 模式跳过蓝图生成
- `docs/技术解决总结/LangGraph图拓扑总结.md`: 更新拓扑图、节点详解、路由、状态流转、文件索引

### 新拓扑
```
classifier → (intent=chat) → chat → END
           → (intent=generate) → skeleton → [gen→val] → merge → final_validate → callback
```
- 已修 AIChatPanel.vue：① 消息底部锚定(::before flex 占位) ② 思考面板改绝对浮动覆盖(top:44px) ③ 节点思考无实时流时回退显示 `reasoning_preview`。报告：.workbuddy/ai-chatpanel-review-2026-08-06.md
- 待确认：用户需在浏览器控制台验证是否出现 `[agentStore] appendThinking node=...` 日志，以判断模型是否回流 reasoning_content。

## 前端代码修复实施（2026-08-06）
- **F1 删墙引用级联**：scenePatch.ts - remove_element 收集所有引用目标元素的子节点（parentWall/constraints.target）一并删除，解决"删墙被拒"问题
- **F2 重建 Gate**：sceneStore.ts - applyPatch 先重建（smoke test）成功后才提交 blueprint/revision，失败则回滚并返回 false
- **F3 材质泄漏**：renderEntity.ts - 占位 mesh 补 `userData.ownsMaterial = true`
- **F4 编译诊断级别**：sceneValidator.ts - 保留诊断原始级别（error 阻断 / warning 提示），不再统一降级
- **F5 确认/拒绝 UI**：AIChatPanel.vue - 增加"拒绝"按钮（requires_confirmation 时显示），接 confirmPatch/rejectPatch
- **F6 agent_reply JSON 解析**：agentBridge.ts - 解析 ```json 内嵌 Blueprint 并自动 loadBlueprint

## 后端代码修复实施（2026-08-06）
- **B1 致命错误拦截**：ws_agent.py - Blueprint 路径增加 has_error 步骤拦截，不下发坏蓝图（与 Patch 路径策略一致）
- **B3 协议注释修正**：ws_agent.py - blueprint_generated 注释对齐实际实现（file_url 模式）
- **B4 类型保护**：spatial_tools.py - 新增 _safe_coords() 工具函数，validate_opening_coords 中捕获非数值坐标异常
- **B6 死代码接线**：agent_service.py - 导入并调用 extract_patch_from_text 替代原始 json_data 判断
- **B8 RAG 降级告警**：agent_service.py - RAG → FileSpecLoader 降级时增加明确 WARNING 日志和排查指引
- **B9 死配置激活**：spatial_tools.py - fix_element_dimensions 增加 floor/stair 修正分支 + column/opening 通用兜底
- 文件：ws_agent.py, spatial_tools.py, agent_service.py
- 重新阅读关键源码文件，验证了 `.workbuddy/` 下分析文档中的核心缺陷（B1/F1/F2/F3/B4）均准确。
- 编写了三份技术文档，输出至 `docs/技术解决总结/`：
  - README.md（汇总索引）
  - 前端缺陷分析与修复方案.md（F1-F9 共 9 项）
  - 后端缺陷分析与修复方案.md（B1-B9 共 9 项）
- 每项缺陷均含：代码定位(file:line)、影响分析、代码级修复方案。
- 建议实施顺序：先对齐文档（B2/B5）→ 修高优先（F1→F2→B1）→ 补健壮性（B4/F3/F4/F5）→ 远期。

## 骨架→子节点信息链修复（2026-08-06 13:38）
- 根据深度分析的方案 A+B+C+D 组合，修改了 5 个文件，让骨架通过 design_brief 传递 facade_plan + component_quota + rag_reference。
- **修改文件**：
  - `graph_state.py`: 添加 design_brief 字段
  - `prompts.py`: build_skeleton_prompt 要求输出 DESIGN_BRIEF JSON（facade_plan+quota），build_component_prompt 接收 design_brief 生成三段约束（quota_section, facade_section, rag_section）
  - `skeleton_node.py`: 添加 _parse_design_brief 解析 DESIGN_BRIEF:，_build_skeleton_summary 去掉机械均布改为输出设计清单 + from坐标约束
  - `base_component_node.py`: _build_user_message 改为按设计清单，generator 传 design_brief 给 prompt
  - `merge_node.py`: 添加 _enforce_component_quota（按主立面优先剃除超额窗）
  - `ws_agent.py`: skeleton 节点 start 记录 design_brief 日志
- **配额强制测试通过**：6窗→配额4→剃除2个非主立面窗，保留2个主立面窗
- **核心逻辑**：骨架不再输出"每面墙3个开口"机械规则，改为区分主立面/侧墙/背面的 facade_plan + 总量 quota。窗节点按 max_openings=0 留空侧墙。合并节点按 quota 最后兜底
- 对比 `D:/Backup/Downloads/AI 生成 - 现代简约别墅.wild`、`D:/Backup/桌面/骨架.txt`（完整思考文本）、RAG知识库（villas.md/windows.md/assembly-templates.md）和 RAG 调用逻辑（loader.py、skeleton_node.py）。
- **核心结论**：RAG 知识库本身质量很好（villas.md §1.1 给了完整 10×8m 现代别墅 Blueprint：1门+2长窗+平屋顶+楼梯+雨棚），但 LangGraph 把它撕成碎片分发到不同节点，每个节点都拿不到完整模板。
- **24 窗的根因**：骨架节点 prompt 中的"建议开口方案"是机械启发式（每面墙 3 个），覆盖了 RAG 原始的"窗×6"绝对数量约束。LLM 思考文本明确显示这一点（行 188：骨架建议每面墙 3 个开口，我就每面墙生成 3 个窗）。
- **RAG 调用 4 个缺陷**：①骨架节点没检索窗户/门款型 ②RAG 最小可行 Blueprint 在 prompt 里只是参考 ③组件节点 RAG 切片太碎 ④metadata_filter 的 status 字段可能屏蔽 proposed 内容。
- **5 个修复方案**：A=骨架节点生成完整 Blueprint / B=骨架输出 facade_plan / C=RAG 模板作为强参考 / D=合并节点加窗墙比校验 / E=A+C+D 组合。
- 输出报告：`docs/技术解决总结/RAG与生成质量深度分析.md`。
- 读取 `D:/Backup/Downloads/现代别墅.wild` 蓝图文件，结合 3 张渲染截图做问题分析。
- 主要问题：屋顶尺寸过大(span=9/depth=7 vs 建筑 8×6)、屋脊过高(y=9)、栏杆嵌在二层墙中(parentFloor=floor_second, y=3, 高1.1)、24个窗户机械均匀分布、后门 from[2] 与 parentWall 不匹配、雨棚与门错位、缺少楼梯、8根角柱冗余。
- 根本原因：组件数量规则过于机械化、坐标校验不足、LLM 缺乏现代住宅设计常识、RAG/prompt 未编码“栏杆只在开放边缘”“雨棚覆盖门洞”等约束。
- 输出报告：`docs/技术解决总结/生成别墅问题分析报告.md`，含问题清单、优先级表、修复方向。
- 修复建议：后端 deepseek-pro 修改 prompts / component_registry / 校验工具，强化现代别墅设计约束。

- 工作区有 BOOTSTRAP.md（身份初始化文件）未处理，用户尚未做身份设定。
