# WildAgent 与通用 Agent 对比及架构简化思考

> 记录时间：2026-09-05  
> 文档性质：项目定位复盘、架构审视与后续验证方案  
> 当前结论：WildAgent 的垂直领域定位成立，但其复杂工作流尚未证明优于通用 Coding Agent，节点设计需要按实际收益进行收缩。

## 一、问题来源

在使用 WorkBuddy 体验 WildAgent 项目时，通用 Agent 能够读取项目源码，识别现有渲染引擎和 API，编写 HTML/JavaScript 调用代码，并渲染出视觉效果较好的建筑。

这带来了一个必须正视的问题：

> 如果通用 Coding Agent 通过“读取项目、编写代码、调用 API、观察结果”的短链路就能完成建筑生成，那么 WildAgent 是否仍有必要维护复杂的 LangGraph 多节点流程、建筑知识库和多阶段生成链路？

这个问题不能通过“WildAgent 使用了更多技术”回答，而应通过产品边界和对照实验回答。

## 二、核心判断

### 2.1 WildAgent 与通用 Agent 的定位不同

WorkBuddy 一类通用 Coding Agent 更像临时软件工程师：

```text
读取仓库
→ 理解接口和示例
→ 动态编写 HTML/JavaScript
→ 调用渲染 API
→ 运行并观察结果
→ 根据错误继续修改
```

WildAgent 的目标则不应只是“也能生成一个建筑”，而应是：

```text
用户自然语言需求
→ 结构化需求与设计状态
→ 受约束的平面和建筑方案
→ 可校验、可修复的 Blueprint
→ 可视化审核和局部修改
→ 可保存、可继续编辑的 .wild 项目
```

因此，两者真正的差别不在于是否调用大模型，而在于最终产物是否具有以下性质：

- 普通用户不需要读取源码或编写代码；
- 建筑产物拥有稳定的结构化语义，而不是一次性页面；
- 设计结果可以保存、继续编辑和版本化；
- 空间、宿主、楼层和几何约束可以被程序验证；
- 用户修改局部内容时，不会无故重生成整栋建筑；
- 多次生成具有可接受的稳定性、成本和延迟；
- 模型只能通过受限工具修改事实状态，行为可以追踪和回滚。

### 2.2 底层技术并不独有

WildAgent 使用的 LangGraph、RAG、结构化输出、Validator、Checkpoint 和 Human-in-the-loop 都属于成熟的 Agent 工程手段，本身不是独家创新。

项目可能形成的差异化，应来自这些能力在建筑领域中的组合与落地：

- `RequirementSpec` 对用户需求的结构化表达；
- `FloorPlanIR` 对平面规划事实的统一维护；
- Blueprint 与 `.wild` 的领域契约；
- 建筑空间关系与组件宿主规则；
- 确定性布局、组装、几何校验和有限修复；
- SVG、FloorPlanIR 和最终 Blueprint 之间的一致性；
- 面向非开发者的可视化修改闭环。

如果这些能力没有真正实现或无法通过指标证明收益，那么 WildAgent 目前就只是一个比通用 Agent 更复杂的生成流程，而不是更有价值的垂直 Agent。

## 三、为什么通用 Coding Agent 的短链路可能效果更好

### 3.1 它直接利用了整个代码仓库作为上下文

通用 Coding Agent 可以读取真实类型、API、调用示例和运行错误。相比之下，WildAgent 当前部分节点依赖 Prompt 摘要和 RAG 片段，可能拿不到最准确的引擎能力边界。

### 3.2 动态代码比过早固定的中间结构更灵活

HTML/JavaScript 可以根据当前任务自由组织调用。WildAgent 的模型输出则要连续经过结构化解析、状态传递、Normalizer、组件配额、确定性装配和 fallback，设计意图可能在过程中被压缩或覆盖。

### 3.3 通用 Agent 可以形成真实的工具反馈循环

当代码报错或页面没有正确渲染时，Coding Agent 可以继续查看错误并修改代码。WildAgent 虽然拥有多个节点，但当前并非所有阶段都形成了“执行结果 → 结构化错误 → 定向修复 → 全量复检”的闭环。

### 3.4 单次视觉效果的验收门槛较低

一次演示通常只需要“页面能运行且看起来不错”。产品化的建筑系统还要处理：

- 结构是否合法；
- 组件是否可以选中和编辑；
- 门窗是否拥有有效宿主；
- 多层建筑关系是否正确；
- 修改后能否保留未修改内容；
- 文件能否重新打开和继续维护；
- 大量不同请求是否都能稳定工作。

因此，单次效果很好不等于完整产品能力更强，但它足以证明 WildAgent 必须建立通用 Agent 基线，不能只和自身历史版本比较。

## 四、当前节点设计审视

当前 `wild-server/app/agent/graph.py` 构建的生成图包含：

- 意图分类、问答和 Patch；
- 研究、规划、计划校验、计划审核和计划执行；
- 建筑方案、平面规划、材料规划；
- 骨架、风格审核和装饰组装；
- 11 类组件各自独立的 `gen → val` 链；
- 合并、最终校验和可选 callback。

按当前注册表计算，图构建时约注册 44 个节点，启用 callback 时约 45 个。实际一次任务不一定经过所有节点，但图的维护面、路由状态和测试组合已经很大。

平面阶段又被拆为：

```text
floor_space_analysis
→ floor_layout
→ floor_openings
→ floor_validate
→ floor_plan_review
```

前三个阶段分别承担 LLM 生成，导致：

- 同一设计意图在多个 Prompt 和状态之间传递；
- 上游错误会逐步放大；
- 模型重复读取相近上下文；
- Token、延迟和解析失败点增加；
- 修改意见需要从较早阶段重新执行；
- 节点数量增加，但最终几何正确性没有同步提高。

因此，当前项目确实存在节点拆分过细、模型职责过多的问题。

## 五、不是“节点越少越好”

节点数量不是目标。一个步骤只有在至少满足以下一项时，才值得成为独立 Graph 节点：

1. 需要独立 Checkpoint，服务重启后必须从这里恢复；
2. 需要独立重试，并且重试不会破坏其他已完成结果；
3. 存在明确的人工确认或中断边界；
4. 具有不同的工具权限、安全边界或副作用；
5. 可以带来真实并行收益；
6. 输入输出拥有稳定、可版本化的状态契约；
7. 失败后需要进入不同的业务路由；
8. 需要独立监控成本、延迟或成功率。

如果一个步骤只是：

- 换了一段 Prompt；
- 多生成一段自然语言 reasoning；
- 对上一节点结果做普通字段转换；
- 执行一个不会独立重试的校验函数；
- 为了展示流程而拆分；

那么它通常更适合作为普通函数、领域服务或同一节点内部的处理阶段，而不是独立 LangGraph 节点。

需要特别明确：

> LLM 调用不必天然对应一个节点，业务步骤也不必全部暴露为 Graph 节点。

## 六、建议的简化方向

### 6.1 平面规划链路

不建议继续让三个 LLM 节点分别手工生成空间、坐标和洞口。可以收缩为：

```text
需求解析与方案规划（LLM）
→ RequirementSpec + SpaceProgram + AdjacencyGraph
→ 布局与开口求解（确定性程序）
→ FloorPlanIR
→ 几何校验（程序）
→ 用户审核
```

LLM 负责功能、关系、偏好和候选决策；程序负责可计算的坐标、共享边界、墙体、洞口和连通性。

### 6.2 组件生成链路

当前 11 类组件分别展开为 `gen → val` 节点，适合真正需要独立并发、独立重试的高差异组件，但不应默认让所有组件都调用模型。

可以调整为：

- 门、窗、栏杆等强约束组件由 Approved Plan 确定性装配；
- 风格、装饰和少数弱约束组件允许模型提出候选；
- Validator 保留为共享领域函数；
- 只有需要独立重试或独立路由的校验才升级为 Graph 节点；
- 多个组件可以在一个受控装配节点中批量处理。

### 6.3 计划层

当前研究、Planner、计划校验、计划审核、Executor 对开放式复杂任务有意义，但建筑生成主链本身已经相对固定。默认生成不一定需要再由模型规划一次固定节点顺序。

建议：

- 普通建筑生成走固定、短链路；
- 只有涉及外部研究、文件分析或复杂编辑任务时才启用动态计划模式；
- 计划校验尽量使用程序规则，不再额外增加无收益的模型调用。

## 七、建议的目标主链

一个更合理的初始目标可以控制在约 8～10 个有明确边界的节点：

```text
1. classify_or_route
2. parse_requirements
3. design_architecture_and_space
4. solve_floor_geometry
5. validate_floor_plan
6. floor_plan_review
7. assemble_blueprint
8. final_validate
9. repair_or_terminate
```

材料和风格是否独立成节点，取决于是否需要单独审核、重试和版本化，而不是为了流程看起来完整。

这个数量只是初始方向，不是硬指标。最终应通过对照实验决定合并或保留哪些节点。

## 八、必须建立的三组基线

为了验证复杂度是否值得，应使用相同建筑需求比较三套方案：

### A. 通用 Coding Agent 基线

允许 Agent 读取项目、编写 HTML/JavaScript 并调用渲染 API。

### B. 当前 WildAgent

使用现有多节点 LangGraph、RAG、组件生成和校验流程。

### C. 简化后的垂直 Agent

使用一个主要规划 Agent、FloorPlanIR、确定性几何求解、Blueprint 装配和有限修复。

每个需求至少重复运行三次，比较：

- 首次渲染成功率；
- 几何校验通过率；
- 用户硬约束满足率；
- 建筑结果多样性；
- 修改保真度；
- 最终产物可编辑性；
- 平均模型调用次数；
- Token 消耗和端到端延迟；
- fallback 触发率；
- 人工介入次数；
- 多次运行稳定性。

## 九、可能得到的三种结论

### 结论一：通用 Agent 视觉效果好，但产物不可持续编辑

说明 WildAgent 的结构化事实源、验证和产品化路线具有价值。

### 结论二：简化版垂直 Agent 最好

说明垂直领域能力有价值，但原多节点架构过度设计，应当收缩。

### 结论三：通用 Agent 在质量、成本、稳定性和编辑性上全面胜出

需要重新评估 WildAgent 的产品边界。项目可以转为提供建筑引擎、Blueprint 工具、Validator、Skill 或 MCP 服务，由通用 Agent 负责规划和调用，而不是继续维护完整的自有 Agent 工作流。

## 十、作为 Agent 难点的分类

- **问题名称**：垂直 Agent 价值边界与架构复杂度收益验证不足
- **一级领域**：Agent Architecture & Evaluation
- **二级领域**：Vertical Agent Design、Build vs. Buy、Complexity–Value Alignment、Baseline Evaluation

这个问题的面试价值在于：没有因为已经使用 LangGraph 和 RAG 就继续堆叠节点，而是引入通用 Agent 基线，重新评估模型、工作流和领域程序的职责边界。

## 十一、面试表达

> 我在项目中发现，通用 Coding Agent 读取仓库后，也能直接调用渲染引擎生成效果不错的建筑。这说明单次生成能力并不足以证明垂直 Agent 的价值，也促使我重新审视多节点架构是否过度设计。我把通用 Agent、现有多节点流程和简化后的结构化 Agent 设为三组基线，从约束满足率、几何正确率、修改保真度、可编辑性、稳定性、成本和延迟进行比较。节点只有在具备独立状态契约、重试、权限、人工审核或并行价值时才保留；模型负责设计决策，程序负责确定性几何、校验和装配。项目真正需要保留的不是 LangGraph 节点数量，而是建筑领域的结构化状态和可验证能力。

## 十二、最终结论

通过这次问题，可以判断：

1. WildAgent 当前没有必要维持这么多以 LLM 分工为主的细粒度节点；
2. 平面分析、布局和洞口不应继续依赖多个模型串行手算；
3. 组件 Validator 可以继续存在，但多数不必成为独立 Graph 节点；
4. 人工审核、最终校验、有限修复和持久化边界仍适合保留为节点；
5. 是否删减任何具体节点，最终要由基线实验和运行指标决定；
6. 项目的差异化应建立在 FloorPlanIR、Blueprint 契约、确定性几何和持续编辑能力上，而不是节点数量上。

一句话概括：

> 不是 WildAgent 不需要工作流，而是它需要更少的模型编排节点、更强的结构化状态，以及更多确定性领域能力。
