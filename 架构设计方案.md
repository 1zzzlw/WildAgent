# WILD 建筑编辑器 - 从零开发最终架构设计方案

## 0. 文档目标

这是一份面向“从 0 开始开发前端和后端”的完整架构设计文档。

目标是：开发者或 AI 仅凭这一份文档，就能理解要做什么、保留什么、重建什么、各模块怎么通信、前后端目录怎么组织、接口怎么设计、开发顺序怎么推进。

本项目唯一保留并复用的现有核心资产是：

```text
wild-core 渲染/重建引擎
```

它负责把 `.wild` 蓝图重建为可渲染的几何数据。新项目不重新设计渲染引擎，不让 AI 生成 Three.js 几何算法，不让后端参与三角形计算。

新项目要从 0 开发的是：

- 前端建筑编辑器。
- 后端 Agent 服务。
- 前后端通信协议。
- 场景状态管理。
- `.wild` 编辑、校验、保存、导出工作流。
- AI 对话生成和修改 `.wild` 的工具链。

## 1. 产品定义

WILD 建筑编辑器是一个 AI 辅助的参数化 3D 建筑编辑器。

它不是“一句话生成死模型”的工具，而是一个可以持续编辑的建筑设计工作台：

```text
用户输入自然语言
  -> AI 生成 .wild 初稿或修改建议
  -> 编辑器展示 3D 结果
  -> 用户选择构件、拖拽、调参、确认或撤销
  -> .wild 文件持续作为唯一源文件
  -> wild-core 根据 .wild 确定性重建几何
```

核心原则：

| 原则 | 说明 |
|---|---|
| `.wild` 是唯一场景源文件 | 所有建筑结构、材质、行为最终都落到 `.wild` JSON |
| 渲染引擎只复用不重做 | `wild-core` 是现有核心引擎，新系统只接入它 |
| AI 不直接生成三角形 | AI 只生成参数、构件、材质、patch |
| 用户保留最终决策权 | 批量修改、删除、覆盖场景必须用户确认 |
| 手动编辑不走 Agent | 拖拽、属性面板、场景树操作直接修改本地状态 |

## 2. 已有资产与边界

当前项目中已有：

```text
wild-lang/              WILD 语言规范、schema、types
wild-core/              现有渲染/重建引擎
wild-viewer/            现有查看器，可作为参考，不作为最终编辑器
lantu/                  示例 .wild 文件
```

### 2.1 必须保留

`wild-core` 的职责保持不变：

```text
输入: Blueprint(.wild JSON)
输出: ReconstructedEntity
      - meshes
      - materialParams
      - boundingBox
      - physics
      - scripts
      - animation
```

现有核心入口：

```ts
parseBlueprint(json: string): Blueprint
reconstructEntity(bp: Blueprint): Promise<ReconstructedEntity>
```

新前端只需要调用这两个入口，然后把 `MeshData` 转成 Three.js 对象。

### 2.2 可以参考但不直接作为最终产品

`wild-viewer` 当前是查看器，不是完整编辑器。它可以作为以下内容的参考：

- 如何加载 `.wild` 文件。
- 如何调用 `parseBlueprint()` 和 `reconstructEntity()`。
- 如何把 `MeshData` 转换成 Three.js `BufferGeometry`。
- 如何处理基础点击交互。

但最终编辑器需要从 0 设计状态管理、组件、AgentBridge、属性面板、构件库、撤销重做等能力。

### 2.3 不允许重做

新项目不做这些事：

- 不重写 `wild-core` 的几何生成算法。
- 不让后端渲染 `.wild`。
- 不让 Agent 输出 Three.js 代码。
- 不把 GLB、OBJ、ThreeJSON 作为主资产格式。
- 不把 UI 状态混入几何语义。

## 3. 总体技术选型

| 层级 | 技术 | 说明 |
|---|---|---|
| 前端应用 | Vue 3 + Vite + TypeScript | 从 0 构建完整编辑器 |
| 前端状态 | Pinia | 管理 Blueprint、选择、撤销、Agent 消息 |
| 3D 渲染 | Three.js | 只负责显示 `wild-core` 输出 |
| 渲染引擎 | 现有 `wild-core` | 不重做，只通过适配层接入 |
| 后端 API | Python FastAPI | 提供 Agent、保存、校验、会话服务 |
| Agent 编排 | LangGraph + LangChain | 单 Agent + 多工具 |
| 前后端实时通信 | WebSocket | Agent 流式响应和 patch 推送 |
| 普通接口 | REST | 场景保存、加载、导出、校验 |
| 初期存储 | 文件系统 JSON | MVP 简单可靠 |
| 后续存储 | SQLite/PostgreSQL + 对象存储 | 多用户、多版本后再升级 |

## 4. 新项目推荐目录结构

在当前仓库基础上扩展：

```text
wild-yuan-backup/
├── wild-lang/                         # 已有：语言规范，不重建
├── wild-core/                         # 已有：渲染/重建引擎，不重建
├── wild-viewer/                       # 已有：查看器参考，可保留
├── lantu/                             # 已有：示例 .wild
│
├── wild-editor/                       # 新建：完整前端编辑器
│   ├── package.json
│   ├── vite.config.ts
│   ├── tsconfig.json
│   ├── index.html
│   └── src/
│       ├── main.ts
│       ├── App.vue
│       ├── app/
│       │   ├── layout/
│       │   └── routes/
│       ├── components/
│       │   ├── viewport/
│       │   │   ├── CanvasViewport.vue
│       │   │   ├── ViewToolbar.vue
│       │   │   └── TransformGizmo.vue
│       │   ├── panels/
│       │   │   ├── SceneTree.vue
│       │   │   ├── PropertyPanel.vue
│       │   │   ├── BlockLibrary.vue
│       │   │   └── AIChatPanel.vue
│       │   └── common/
│       ├── stores/
│       │   ├── sceneStore.ts
│       │   ├── selectionStore.ts
│       │   ├── historyStore.ts
│       │   ├── agentStore.ts
│       │   └── uiStore.ts
│       ├── wild/
│       │   ├── scenePatch.ts
│       │   ├── sceneValidator.ts
│       │   ├── sceneSummary.ts
│       │   ├── idFactory.ts
│       │   └── blueprintDefaults.ts
│       ├── renderer/
│       │   ├── wildCoreAdapter.ts
│       │   ├── renderEntity.ts
│       │   ├── meshDataToGeometry.ts
│       │   ├── materialAdapter.ts
│       │   └── picking.ts
│       ├── agent/
│       │   ├── agentBridge.ts
│       │   ├── protocol.ts
│       │   └── messageTypes.ts
│       └── services/
│           ├── sceneApi.ts
│           └── fileExport.ts
│
├── wild-agent/                        # 新建：后端 Agent 服务
│   ├── pyproject.toml
│   ├── README.md
│   └── app/
│       ├── main.py
│       ├── config.py
│       ├── api/
│       │   ├── ws_agent.py
│       │   ├── scenes.py
│       │   └── validate.py
│       ├── agent/
│       │   ├── graph.py
│       │   ├── state.py
│       │   ├── prompts.py
│       │   └── model_client.py
│       ├── tools/
│       │   ├── scene_read_tools.py
│       │   ├── scene_patch_tools.py
│       │   ├── validation_tools.py
│       │   └── design_tools.py
│       ├── schemas/
│       │   ├── blueprint.py
│       │   ├── scene_patch.py
│       │   └── protocol.py
│       ├── services/
│       │   ├── session_service.py
│       │   ├── scene_service.py
│       │   ├── validation_service.py
│       │   └── storage_service.py
│       └── storage/
│           └── scenes/
│
└── docs/
    ├── 最终架构设计方案-从零开发版.md
    └── WILD建筑编辑器开发规范.md
```

## 5. 核心数据模型

### 5.1 Blueprint

`.wild` 文件本质是一个 JSON Blueprint。

最小结构：

```json
{
  "meta": {
    "version": "1.0",
    "type": "building",
    "name": "未命名建筑"
  },
  "geometry": {
    "elements": []
  },
  "materials": {},
  "behaviors": {}
}
```

标准顶层字段：

| 字段 | 是否必需 | 说明 |
|---|---|---|
| `meta` | 是 | 版本、类型、名称 |
| `geometry` | 是 | 构件、模板、实例、批量放置 |
| `materials` | 否 | 材质库 |
| `behaviors` | 否 | 物理、脚本、动画 |
| `editor` | 否 | 编辑器私有元数据，不影响渲染 |

### 5.2 Geometry

```json
{
  "geometry": {
    "elements": [],
    "templates": {},
    "instances": [],
    "placements": []
  }
}
```

标准构件类型：

| type | 用途 |
|---|---|
| `wall` | 墙体 |
| `floor` | 地板/平台 |
| `column` | 柱子 |
| `beam` | 梁 |
| `roof` | 屋顶 |
| `opening` | 门窗洞口 |
| `stair` | 楼梯 |
| `furniture` | 参数化家具和瓦片 |
| `dense_brick` | 高分辨率体素细节 |
| `body` | 化身身体 |

### 5.3 SceneDocument

前端编辑器内部不只保存 Blueprint，还要保存编辑会话状态。

```ts
interface SceneDocument {
  id: string
  name: string
  revision: number
  blueprint: Blueprint
  dirty: boolean
  savedAt?: number
}
```

`revision` 是前后端协作的关键。每次成功应用 patch 后递增。

### 5.4 Editor Metadata

不影响引擎输出的编辑器信息放在 `blueprint.editor`：

```json
{
  "editor": {
    "version": "1.0",
    "groups": {
      "main_house": {
        "name": "主体建筑",
        "elements": ["wall_front", "wall_back", "main_roof"]
      }
    },
    "view": {
      "camera": {
        "position": [12, 10, 12],
        "target": [0, 1.5, 0]
      }
    },
    "agent": {
      "generatedBy": "wild-agent",
      "lastPrompt": "生成一座中式凉亭"
    }
  }
}
```

硬规则：

- `editor` 不能影响 `wild-core` 的重建结果。
- `wild-core` 不能读取 `editor` 字段。
- 导出纯资产时可以剥离 `editor`。

## 6. ScenePatch 协议

所有编辑操作，无论来自用户还是 Agent，都转成 `ScenePatch`。

### 6.1 Patch 基本结构

```ts
interface ScenePatch {
  type: 'scene_patch'
  patch_id: string
  base_revision: number
  source: 'user' | 'agent' | 'system'
  mode: 'apply' | 'proposal'
  requires_confirmation: boolean
  operations: SceneOperation[]
  summary?: string
}
```

### 6.2 操作类型

```ts
type SceneOperation =
  | { op: 'add_element'; element: GeometryElement }
  | { op: 'update_element'; id: string; changes: Record<string, unknown> }
  | { op: 'remove_element'; id: string }
  | { op: 'upsert_material'; name: string; material: MaterialDef }
  | { op: 'add_template'; name: string; template: GeometryElement }
  | { op: 'update_template'; name: string; changes: Record<string, unknown> }
  | { op: 'remove_template'; name: string }
  | { op: 'add_instance'; instance: InstanceRef }
  | { op: 'add_placement'; placement: Placement }
  | { op: 'set_behaviors'; changes: Record<string, unknown> }
  | { op: 'set_editor_meta'; changes: Record<string, unknown> }
```

### 6.3 应用流程

```text
收到 patch
  -> 检查 base_revision 是否等于当前 revision
  -> 校验 operations 格式
  -> 应用到 Blueprint 副本
  -> 运行 Blueprint 校验
  -> 调用 wild-core reconstructEntity 做 smoke test
  -> 成功后替换 SceneStore.blueprint
  -> revision + 1
  -> 渲染刷新
  -> 记录 undo 快照
```

### 6.4 确认规则

| 操作 | 是否需要确认 |
|---|---|
| 属性面板单字段修改 | 否 |
| 拖拽添加构件 | 否 |
| Agent 添加普通构件 | 可配置，默认否 |
| Agent 批量添加 | 是 |
| 删除构件 | 是 |
| 覆盖整个场景 | 是 |
| 一键整理 | 是 |
| 风格迁移 | 是 |

## 7. 前端架构

### 7.1 页面布局

推荐桌面编辑器布局：

```text
┌──────────────────────────────────────────────────────────────┐
│ 顶部工具栏：新建 / 打开 / 保存 / 导出 / 撤销 / 重做 / 运行校验 │
├──────────────┬───────────────────────────────┬───────────────┤
│ 左侧面板      │ 中央 3D 视口                    │ 右侧面板       │
│              │                               │               │
│ 构件库        │ CanvasViewport                 │ 属性面板        │
│ - 基础构件    │ - Three.js                      │ - 按类型渲染表单 │
│ - 模板模块    │ - OrbitControls                 │ - 数值输入       │
│ - AI 生成模块 │ - 选中高亮                       │ - 材质选择       │
│              │ - Gizmo                         │               │
│ 场景树        │                               │ 校验结果        │
│ - 层级/分组   │                               │               │
├──────────────┴───────────────────────────────┴───────────────┤
│ 底部 AI 对话面板：用户输入 / Agent 流式过程 / patch 确认         │
└──────────────────────────────────────────────────────────────┘
```

### 7.2 前端 Store

#### sceneStore

职责：

- 保存当前 `SceneDocument`。
- 应用 `ScenePatch`。
- 调用重建。
- 管理 dirty 状态。

核心状态：

```ts
interface SceneStoreState {
  document: SceneDocument | null
  reconstructed: ReconstructedEntity | null
  validationIssues: ValidationIssue[]
  isReconstructing: boolean
}
```

核心方法：

```ts
loadBlueprint(bp: Blueprint): void
applyPatch(patch: ScenePatch): Promise<void>
reconstruct(): Promise<void>
validate(): ValidationIssue[]
exportWild(): string
```

#### selectionStore

职责：

- 当前选中构件。
- hover 构件。
- 多选。

```ts
interface SelectionStoreState {
  selectedIds: string[]
  hoveredId: string | null
}
```

#### historyStore

职责：

- 撤销。
- 重做。
- patch 历史。

```ts
interface HistoryEntry {
  label: string
  before: Blueprint
  after: Blueprint
  patch: ScenePatch
}
```

#### agentStore

职责：

- 聊天消息。
- WebSocket 状态。
- Agent 正在执行的步骤。
- 等待用户确认的 patch。

### 7.3 渲染适配层

前端不能把渲染转换逻辑散落在 Vue 组件里，必须集中在 `renderer/`。

```text
renderer/wildCoreAdapter.ts
  - import wild-core
  - parseBlueprintText()
  - reconstructBlueprint()

renderer/meshDataToGeometry.ts
  - MeshData -> THREE.BufferGeometry

renderer/materialAdapter.ts
  - MaterialParams -> THREE.Material

renderer/renderEntity.ts
  - ReconstructedEntity -> THREE.Group

renderer/picking.ts
  - raycast result -> elementId
```

重建流程：

```text
SceneStore.blueprint
  -> wildCoreAdapter.reconstructBlueprint()
  -> ReconstructedEntity
  -> renderEntity()
  -> CanvasViewport 替换场景 group
```

MVP 可以整场重建。后期再做局部重建和缓存。

### 7.4 属性面板

属性面板根据选中构件的 `type` 动态渲染。

示例：

| 构件 | 控件 |
|---|---|
| `wall` | from/to 坐标、厚度、材质、曲线 |
| `column` | base、height、bottomRadius、topRadius、style、材质 |
| `roof` | roofType、span、depth、height、thickness、tiers、材质 |
| `opening` | parentWall、from、width、height、style、材质 |
| `furniture` | subtype、position、dimensions、style、材质 |

属性变更时生成 patch：

```ts
{
  op: 'update_element',
  id: 'col_03',
  changes: { height: 3.8 }
}
```

### 7.5 构件库

构件库分两类：

```text
预设构件
  - wall
  - floor
  - column
  - beam
  - roof
  - opening
  - stair
  - furniture

AI 生成模块
  - 凉亭柱网
  - 门楣模板
  - 瓦片 placement
  - 中式屋顶组合
```

拖拽到视口时：

```text
drag start
  -> 创建构件草稿
drop on viewport
  -> 根据拾取点计算位置
  -> 生成 add_element 或 add_instance patch
  -> applyPatch
```

## 8. 后端架构

后端只负责 Agent、会话、保存、校验，不负责渲染。

### 8.1 服务模块

```text
FastAPI app
├── WebSocket Agent API
├── Scene REST API
├── Validate REST API
├── LangGraph Agent
├── Tool Layer
├── Session Service
├── Scene Storage
└── Validation Service
```

### 8.2 REST API

#### 创建场景

```http
POST /api/scenes
```

请求：

```json
{
  "name": "新建筑",
  "blueprint": {
    "meta": { "version": "1.0", "type": "building", "name": "新建筑" },
    "geometry": { "elements": [] },
    "materials": {}
  }
}
```

响应：

```json
{
  "scene_id": "scene_001",
  "revision": 1
}
```

#### 获取场景

```http
GET /api/scenes/{scene_id}
```

响应：

```json
{
  "scene_id": "scene_001",
  "revision": 8,
  "blueprint": {}
}
```

#### 保存场景

```http
PUT /api/scenes/{scene_id}
```

请求：

```json
{
  "revision": 8,
  "blueprint": {}
}
```

#### 校验场景

```http
POST /api/validate
```

请求：

```json
{
  "blueprint": {}
}
```

响应：

```json
{
  "valid": true,
  "issues": []
}
```

### 8.3 WebSocket Agent API

连接地址：

```text
ws://localhost:8000/ws/agent
```

#### 前端发送用户消息

```json
{
  "type": "user_message",
  "request_id": "req_001",
  "session_id": "sess_001",
  "scene_id": "scene_001",
  "scene_revision": 8,
  "message": "在别墅右侧加一个中式凉亭",
  "scene_summary": {
    "elements_count": 42,
    "types": ["wall", "floor", "roof", "opening"],
    "bbox": { "min": [-8, 0, -6], "max": [8, 8, 10] }
  },
  "selection": []
}
```

#### 后端返回流式状态

```json
{
  "type": "agent_step",
  "request_id": "req_001",
  "stage": "inspecting",
  "content": "正在读取当前建筑边界和可放置区域。"
}
```

#### 后端返回 patch 提案

```json
{
  "type": "patch_proposal",
  "request_id": "req_001",
  "patch": {
    "type": "scene_patch",
    "patch_id": "patch_001",
    "base_revision": 8,
    "source": "agent",
    "mode": "proposal",
    "requires_confirmation": true,
    "operations": [],
    "summary": "将在别墅右侧添加四根红木柱和一个双层中式屋顶。"
  }
}
```

#### 后端返回最终文本

```json
{
  "type": "agent_reply",
  "request_id": "req_001",
  "content": "我已生成凉亭方案。请确认后应用到场景。"
}
```

## 9. Agent 架构

当前阶段使用单 Agent，不使用多 Agent。

### 9.1 原因

当前任务域集中在一份 `.wild` 文件上：

- 生成建筑。
- 添加构件。
- 修改参数。
- 生成模板。
- 给设计建议。
- 校验合理性。

这些不需要多个 Agent 协作。多 Agent 会增加通信、冲突合并、调试和延迟成本。

### 9.2 LangGraph 状态图

```text
用户消息
  -> load_context
  -> classify_intent
  -> inspect_scene
  -> plan_patch
  -> validate_patch
  -> respond
```

节点说明：

| 节点 | 职责 |
|---|---|
| `load_context` | 读取用户消息、场景摘要、当前选择 |
| `classify_intent` | 判断是新建、添加、修改、删除、建议、解释 |
| `inspect_scene` | 需要时调用读取工具获取元素详情 |
| `plan_patch` | 生成完整场景或 ScenePatch |
| `validate_patch` | 校验 patch 是否符合规则 |
| `respond` | 返回自然语言说明和 patch |

### 9.3 Agent State

```py
class AgentState(TypedDict):
    session_id: str
    request_id: str
    scene_id: str | None
    scene_revision: int
    user_message: str
    scene_summary: dict
    selection: list[str]
    messages: list[dict]
    intent: str | None
    pending_patch: dict | None
    validation_issues: list[dict]
    final_reply: str | None
```

### 9.4 工具清单

读工具：

| 工具 | 输入 | 输出 |
|---|---|---|
| `inspect_scene` | scene_summary | 场景概览 |
| `list_elements` | type? | 元素摘要列表 |
| `get_element_info` | element_id | 元素完整参数 |
| `list_materials` | 无 | 材质列表 |
| `get_selection` | 无 | 当前选中元素 |

写工具：

| 工具 | 输入 | 输出 |
|---|---|---|
| `generate_scene` | description | 完整 Blueprint 草稿 |
| `propose_add_element` | type, params | add_element patch |
| `propose_update_element` | id, changes | update_element patch |
| `propose_remove_element` | id | remove_element patch |
| `generate_module` | description | template/instance/placement patch |
| `organize_scene` | scope | 批量整理 patch |
| `suggest_design` | focus | 文本建议 |
| `validate_patch` | patch | 校验结果 |

### 9.5 Agent 行为规则

Agent 必须遵守：

- 所有场景修改都输出 `ScenePatch`。
- 不直接输出 Three.js、HTML、CSS、JS。
- 不生成未知构件类型。
- 不修改 `wild-core`。
- 不生成大段不可校验 JSON。
- 不清楚用户指代时，如果有 selection 就使用 selection，没有 selection 就提问。
- 删除、覆盖、批量整理必须设置 `requires_confirmation: true`。

## 10. 校验体系

### 10.1 前端校验

前端在应用 patch 前执行：

```text
patch 格式校验
  -> revision 校验
  -> Blueprint schema 校验
  -> 语义校验
  -> wild-core 重建 smoke test
```

语义校验包括：

- 构件 id 唯一。
- 材质引用存在。
- `opening.parentWall` 存在且是 `wall`。
- `behavior.constraints.target` 存在。
- `placement.template` 存在。
- 数值字段在合理范围。

### 10.2 后端校验

后端校验用于提前过滤 Agent 错误，但最终是否应用仍由前端决定。

后端需要和前端共用同一套 schema 来源：`wild-lang/schema.json`。

## 11. 文件保存和导出

### 11.1 本地导入

用户可以打开 `.wild` 文件：

```text
File input
  -> read text
  -> parseBlueprint
  -> loadBlueprint
  -> reconstruct
  -> render
```

### 11.2 本地导出

导出 `.wild`：

```text
SceneStore.blueprint
  -> 可选剥离 editor
  -> JSON.stringify pretty
  -> download .wild
```

### 11.3 后端保存

MVP 使用文件存储：

```text
wild-agent/app/storage/scenes/{scene_id}.wild
wild-agent/app/storage/scenes/{scene_id}.meta.json
```

后续多用户再升级数据库。

## 12. 典型用户流程

### 12.1 AI 从零生成建筑

```text
用户: “生成一座中式凉亭，四根红柱，双层屋顶”
  -> 前端发送 user_message + 当前空场景摘要
  -> Agent 生成完整 Blueprint 或 batch patch
  -> 前端展示 patch 预览
  -> 用户确认
  -> applyPatch
  -> wild-core 重建
  -> 视口显示凉亭
```

### 12.2 手动调参

```text
用户点击柱子
  -> picking 得到 elementId
  -> selectionStore.selectedIds = ["col_01"]
  -> PropertyPanel 读取 column 参数
  -> 用户修改 height
  -> 生成 update_element patch
  -> applyPatch
  -> reconstruct
  -> render
```

### 12.3 AI 修改当前选择

```text
用户选中 col_01
用户: “把这根柱子加粗一点”
  -> 前端发送 selection: ["col_01"]
  -> Agent get_element_info("col_01")
  -> Agent 生成 update_element patch
  -> 前端应用或确认
```

### 12.4 AI 一键整理

```text
用户: “帮我把亭子的柱子间距调均匀”
  -> Agent 查找亭子相关 column
  -> 生成 batch update patch
  -> requires_confirmation = true
  -> 用户查看差异并确认
```

## 13. 开发路线图

### Phase 0：项目初始化

目标：把前后端空项目跑起来。

产出：

- `wild-editor` Vite + Vue + TypeScript 项目。
- `wild-agent` FastAPI 项目。
- 前端能启动空页面。
- 后端有 `/health`。

验收：

- `wild-editor` 能打开页面。
- `wild-agent` 返回健康状态。

### Phase 1：前端接入现有渲染引擎

目标：从 0 新建编辑器，但先只做到“加载 `.wild` 并显示”。

产出：

- `wildCoreAdapter.ts`
- `renderEntity.ts`
- `CanvasViewport.vue`
- 文件打开功能。

验收：

- 能加载 `lantu/bieshu.wild`。
- 能加载 `lantu/tiantan.wild`。
- 能显示顶点数、构件数、材质数。

### Phase 2：SceneStore + Patch

目标：建立统一状态修改管道。

产出：

- `sceneStore.ts`
- `scenePatch.ts`
- `historyStore.ts`
- `applyPatch()`
- 撤销/重做。

验收：

- 添加一个柱子。
- 修改柱子高度。
- 撤销修改。
- 重做修改。

### Phase 3：编辑器 UI

目标：可视化编辑 `.wild`。

产出：

- `SceneTree.vue`
- `PropertyPanel.vue`
- `BlockLibrary.vue`
- 选中高亮。
- 拖拽添加基础构件。

验收：

- 点击 3D 构件能选中。
- 属性面板能编辑构件字段。
- 场景树能定位构件。
- 构件库能拖入柱子、墙、屋顶。

### Phase 4：后端 Agent MVP

目标：AI 能生成 patch 提案。

产出：

- FastAPI WebSocket。
- LangGraph 单 Agent。
- Prompt 模板。
- `propose_patch` 工具。
- 前端 `AIChatPanel`。

验收：

- 用户输入“加一根柱子”。
- Agent 返回 `ScenePatch`。
- 前端确认后应用。
- 场景重建并显示。

### Phase 5：完整 AI 编辑工作流

目标：AI 能生成场景、修改选中元素、批量整理。

产出：

- `generate_scene`
- `get_element_info`
- `list_elements`
- `organize_scene`
- patch 校验和确认 UI。

验收：

- 能从空场景生成中式凉亭。
- 能根据选中构件修改参数。
- 批量整理需要确认。
- 错误 patch 不会污染场景。

### Phase 6：保存、导出、稳定化

目标：形成可用产品闭环。

产出：

- 本地导入导出。
- 后端保存。
- 自动保存。
- 校验面板。
- 错误恢复。

验收：

- 用户可以保存 `.wild`。
- 重新打开后场景一致。
- 导出的 `.wild` 能被现有 `wild-viewer` 打开。

## 14. MVP 范围

第一版必须完成：

- 前端完整编辑器壳。
- 加载和渲染 `.wild`。
- SceneStore。
- ScenePatch。
- 选中构件。
- 属性面板编辑。
- 构件库添加基础构件。
- AI 对话生成 patch。
- 用户确认 Agent patch。
- 本地导入导出。

第一版不做：

- 多 Agent。
- MCP。
- RAG。
- 多用户协作。
- 后端渲染。
- 局部重建。
- GLB/IFC/USD 导出。
- 复杂权限系统。

## 15. 最终架构总结

最终系统由三部分组成：

```text
1. wild-core
   已有渲染/重建引擎。
   不重做，不由 Agent 修改。

2. wild-editor
   从 0 开发的前端编辑器。
   负责 UI、状态、patch、调用 wild-core、渲染 Three.js。

3. wild-agent
   从 0 开发的后端 Agent 服务。
   负责理解用户意图，生成 .wild Blueprint 或 ScenePatch。
```

最重要的数据流：

```text
用户操作 / Agent 建议
  -> ScenePatch
  -> SceneStore.blueprint
  -> wild-core.reconstructEntity()
  -> ReconstructedEntity
  -> Three.js 渲染
```

最重要的边界：

```text
Agent 只改参数，不生成几何。
Editor 只管状态，不发明语言。
wild-core 只管重建，不管 UI 和 AI。
.wild 永远是唯一源文件。
```

只要遵守这四条，前端和后端可以从 0 开发，现有渲染引擎可以稳定复用，后续再逐步扩展 MCP、RAG、局部重建和多格式导出。
