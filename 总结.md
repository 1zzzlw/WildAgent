# WildAgent 实习项目总结

> 本文用于统一项目事实、个人职责、简历表述和面试口径。简历只选择其中最有价值的 4～5 条，面试时再展开问题、分析、行动、验证与边界。

## 1. 一句话项目介绍

WildAgent 是面向 3D 虚拟世界建筑内容生产的 AI Agent 工具。在公司已有 `.wild` 参数化语言规范和基础渲染引擎之上，我负责 Agent 层的整体设计与开发，使团队能够通过自然语言生成可校验、可版本化、可继续编辑的建筑 Blueprint，并将结果导入 Web 端和移动端 3D 世界完成渲染。

## 2. 项目背景与团队边界

### 2.1 公司原有基础

我接手项目时已经具备：

- `.wild` 参数化建筑文件的语言规范；
- 将 `.wild` 编译为 Three.js 可解析数据的基础渲染引擎；
- Java 后端管理平台；
- Web 端和移动端的 3D 世界页面。

这些能力解决了“建筑怎样描述、怎样渲染、怎样进入 3D 世界”的问题，但当时还缺少根据自然语言自动生产建筑蓝图的 Agent 系统。

### 2.2 我的职责

除 `.wild` 语言规范和基础渲染引擎的原始设计外，Agent 相关能力及 CI/CD 自动化部署均由我负责设计和实现，主要包括：

- Agent 整体架构与 LangGraph 工作流；
- 自然语言需求到结构化建筑方案和 `.wild` Blueprint 的生成链路；
- RAG 知识库、语义分片、向量检索、过滤、重排与评测；
- Blueprint 校验、定向修复、复检、回滚和最终保存门禁；
- 长任务持久化、Checkpoint、事件序列和 WebSocket 断线恢复；
- Jenkins、Docker、部署预检、服务就绪检查与失败回滚；
- 基础渲染引擎的后续优化以及与 3D 世界前端的接入。

简历中应使用“负责 Agent 层从 0 到 1 的整体设计与开发”，不再使用容易弱化个人贡献的“参与构建 Agent 工作流”。涉及渲染引擎时，应写“基于并优化已有渲染引擎”，不能写成我从零设计了渲染引擎。

## 3. 为什么采用 `.wild` 参数化建筑

3D 世界需要持续增加并同时管理大量建筑。如果每种建筑都制作和维护一份完整的 GLB 模型，会增加建模、存储、传输、版本管理和后续修改的成本。

`.wild` 使用参数、构件和关系描述建筑，具有以下业务价值：

- 同一套协议可以表达不同体量、楼层、门窗、屋顶、材质和附属构件；
- 文件可以继续编辑和生成 Patch，而不是只能替换完整模型；
- 相同构件和规则能够由渲染引擎统一解释，减少重复制作模型资产；
- 建筑蓝图可以被程序校验、版本化和自动修复；
- 适合作为自然语言 Agent 与 3D 渲染系统之间的结构化协议。

### 3.1 当前仓库样本数据

2026-09-14 对仓库中 150 份 `.wild` 文件进行静态体积统计：

- 中位数约为 `16.97 KB`；
- 140 份（`93.3%`）小于 `50 KB`；
- 145 份（`96.7%`）小于 `100 KB`；
- 少数大型商业综合体超过 `100 KB`，最大约为 `274.69 KB`；
- 84 份名称包含“别墅、住宅或小木屋”的样本全部小于 `33 KB`，中位数约为 `17.36 KB`。

因此可以说“普通住宅蓝图通常只有十几到三十余 KB”或“绝大多数现有样本小于 100 KB”，但不能说“所有 `.wild` 文件都没有超过 50 KB 或 100 KB”。这里统计的是参数化 Blueprint 文件体积，不是 Three.js 展开后的 Mesh 运行内存。

当前仍缺少同一建筑在 GLB 与 `.wild` 表达下的统一文件体积、加载耗时和运行内存对照，因此简历不直接写“内存降低百分之多少”或“一定比 GLB 更小”。更稳妥的表述是：

> 现有普通住宅 `.wild` 蓝图通常为十几到三十余 KB；参数化表达减少了大量预制建筑模型的重复制作与维护，并支持后续编辑、校验和版本管理。

## 4. 为什么不能只调用一次大模型

给大模型提供完整 `.wild` 资料后，它可能生成一份看似可用的参数文件，但“偶尔生成成功”不等于形成了可以稳定交付的生产系统。

### 4.1 单次大模型调用的问题

- `.wild` 协议复杂，模型容易生成不存在的字段、错误枚举或不受支持的构件；
- 建筑包含楼层、墙体、门窗、屋顶、材质和宿主关系，单次输出容易出现坐标与引用不一致；
- 把全部资料放入每次 Prompt 会增加 Token、延迟和无关上下文干扰；
- 模型输出存在随机性，失败后很难判断问题出在需求理解、方案设计、装配还是校验；
- 一个局部错误不应该导致整栋建筑重新生成；
- 长时间生成还涉及用户审核、断线恢复、任务状态和最终保存门禁。

### 4.2 Agent 系统提供的能力

- **工作流编排**：将需求理解、方案设计、组件生成、合并和校验拆分为职责明确的阶段；
- **结构化状态**：使用 LangGraph State 在不同节点之间传递设计方案、几何约束、组件结果和校验信息；
- **RAG 知识约束**：按任务检索 `.wild` 协议、引擎能力和条件组装关系，避免每次注入全部资料；
- **确定性执行**：可以由程序完成的装配、校验和唯一解修复不交给模型猜测；
- **受限纠错**：模型只选择白名单修复动作，程序执行后重新校验，不安全的候选自动回滚；
- **人工审核**：总体方案、平面和风格等需要用户取舍的内容可以暂停确认；
- **可观测和可恢复**：记录节点、模型调用、检索与事件状态，并支持长任务断线续传；
- **交付门禁**：未通过结构、几何和服务就绪检查的结果不能保存或发布。

RAG 的职责不是代替用户“决定建筑应该设计成什么样”，而是向当前阶段提供项目特有且可执行的知识。建筑方向应主要来自用户需求、已批准方案和结构化设计状态。

核心结论：

> Agent 的价值不是让大模型第一次具备生成 `.wild` 的可能，而是把不稳定的一次性模型输出，转化为可编排、可约束、可验证、可纠错、可恢复和可交付的工程系统。

## 5. Agent 总体链路

```text
用户自然语言需求
→ 意图分类（咨询 / 编辑 / 生成）
→ 执行计划与用户审核
→ 建筑总体方案与平面设计
→ 材料规划
→ 主体骨架生成
→ 动态组件生成与校验
→ Blueprint 合并
→ 最终校验
→ 定向修复、复检或回滚
→ 保存 .wild 文件
→ 导入 3D 世界
→ 渲染引擎转换并由 Three.js 展示
```

该链路中，大模型负责需求理解和不可替代的设计判断，确定性程序负责协议归一化、装配、校验、受限修复和交付门禁。

## 6. 五项核心个人成果

### 6.1 Agent 系统从 0 到 1

基于 FastAPI 与 LangGraph 设计并实现 Agent 主链，将原本可能由一次模型调用完成的 JSON 生成，拆分为意图分类、方案规划、分阶段生成、合并、校验和恢复流程。通过结构化 State 传递建筑方案、几何约束、组件结果与校验状态，并使用 LangSmith Trace 和服务日志观察节点执行与模型调用。

在真实生成样本中，定位到已批准约束只进入模型 Prompt、没有继续进入候选评分，导致候选设计偏离已确认方案的规划—执行失配。该案例证明工作流“存在节点”并不等于约束真正贯穿执行链；相关完整结构化设计契约和更广泛量化评测属于后续优化方向，面试中不能说成已经全部解决。

### 6.2 RAG 知识治理与 LangSmith 评测

实现 Markdown 语义分片、Embedding、Chroma 向量检索、metadata 过滤、规则重排、相邻分片补充和检索 Trace。在后续 rules-v3 重构中，将普通生成知识限定为：

- `protocol`：`.wild` 和 ScenePatch 协议；
- `capability`：当前引擎真正支持的构件与字段；
- `relation`：已选择系统和组件之间可执行的组装关系。

建筑百科、建筑类型卡、风格策略、完整建筑案例、fallback 和未实现提案不再进入普通生成上下文，并通过 Loader 白名单、知识 linter、测试和 `required_documents` 清单防止旧模板重新混入。

使用 LangSmith Dataset / Experiment 管理当前 51 题评测集，其中包含 16 题回答样本和 35 题拒绝/隔离样本。回答样本通过 Hit@K、Recall@K 和 MRR 检查标准来源的召回与排序，拒绝/隔离样本检查建筑类型知识、无关领域内容或禁止术语是否被错误注入。LangSmith Trace 用于观察检索和生成链路，Dataset / Experiment 用于保存样本、运行实验和比较结果。当前可以表述为“建立了 LangSmith 评测体系”，不能在没有同一真实 Embedding、索引快照和对照实验的情况下宣称“检索准确率达到 85%”或“已获得最优 Top-K”。

### 6.3 Agent 校验与自我纠错闭环

将最终校验文本转换为逐实体 `ValidationIssue`，保留错误码、校验器、实体 ID、实体类型、修复模式和建议工具。模型不直接重写完整 Blueprint，只能从白名单动作中选择局部修复方式；程序在 Blueprint 副本上执行，随后运行同一套完整校验器复检。

只有错误严格减少且没有引入新的“错误码 + 实体”组合时才提交候选，否则自动回滚。流程同时设置单目标重试预算与最终保存门禁，避免模型无意义循环以及错误 Blueprint 被当成成功结果保存。

当前已经完成的是 Blueprint 层的局部校验—修复—复检—回滚闭环。覆盖全部用户需求的统一设计契约、更多全局错误恢复以及浏览器真实 Mesh 结果自动回传，仍属于进一步完善方向。

### 6.4 长任务、事件持久化与断线恢复

将 LangGraph 生成任务从单条物理 WebSocket 连接中解耦，持久化任务、Checkpoint 和带序号的事件。客户端断线重连后携带最后事件序号，服务端补发缺失事件或恢复未完成任务，避免网络连接中断直接导致整个建筑生成失败。

设计版本化事件协议，将消息、步骤、思考、诊断和产物绑定到 `request_id + session_id`，防止多个请求或会话之间的状态串扰。心跳使用动态任务探针判断连接是否仍关联后台任务，减少长时间模型调用期间的假断连。

### 6.5 CI/CD 与交付门禁

基于 GitLab、Jenkins Pipeline 与 Docker 串联前端构建、后端测试、镜像构建、部署预检、容器启动检查和生产发布。通过知识库 `required_documents` 清单验证镜像是否包含全部正式知识文档，并使用 `/health/ready` 检查当前进程是否真正加载 RAG Loader、基础规范和检索来源。

如果新容器创建失败、启动退出或在限定时间内未达到就绪状态，部署流程自动恢复旧镜像。该机制解决了知识库可能被 `.dockerignore` 排除或被生产目录挂载遮蔽，但普通 HTTP 首页仍显示服务可用的问题。

仓库当前记录的回归基线为后端 `189 passed + 4 subtests`，前端生产构建通过；简历可以作为当前项目测试基线描述，但面试时需要说明它是整个项目测试结果，不是单个功能的效果提升指标。

## 7. 五个难点与完成状态的正确关系

| 难点                     | 已完成部分                                                          | 仍属于优化方案的部分                                   | 简历定位                                               |
| ------------------------ | ------------------------------------------------------------------- | ------------------------------------------------------ | ------------------------------------------------------ |
| Agent 工作流与中间状态   | LangGraph 主链、结构化 State、Trace 与具体失配定位                  | 完整 RequirementSpec、批准设计契约和全链约束兑现率     | 可写已实现的工作流，同时明确失配问题是已定位并形成方案 |
| 知识库职责错位与模板偏置 | rules-v3、角色过滤、活动来源白名单、linter 与清单门禁               | 继续用真实生成对照量化多样性和设计质量                 | 核心已落地亮点                                         |
| RAG 准确率与利用率       | 分片、检索、过滤、重排、LangSmith Trace、51 题 Dataset / Experiment | 真实 Embedding 下完整 A/B、知识采用率和 Blueprint 增益 | 与知识治理合并，不能虚构准确率提升                     |
| Agent 自我纠错           | Blueprint 局部校验、白名单修复、复检、回滚和保存门禁                | 全局错误路由、完整需求一致性和 Mesh 自动反馈           | 核心已落地亮点                                         |
| 平面规划与 FloorPlanIR   | 现状审计、真实失败样本定位和目标方案                                | 拓扑优先求解、FloorPlanPatch、可交互 SVG 和完整版本链  | 更适合作为面试系统设计案例，不写成已完成产品功能       |

“Agent、RAG、自我纠错、长任务和 CI/CD 都由我完成”描述的是项目现有能力的个人所有权；难点文档中额外提出的未来架构目标，不应因为现有模块由我实现而一并描述成已经落地。

## 8. 简历推荐表述

### 8.1 项目介绍

> 面向 3D 虚拟世界的建筑内容生产需求，基于公司已有的 `.wild` 参数化协议与基础渲染引擎，负责从 0 到 1 设计并开发 AI Agent 系统，将自然语言需求转化为可校验、可版本化、可继续编辑的建筑 Blueprint，并通过渲染引擎接入 Web 和移动端 3D 世界。

### 8.2 核心经历

- **Agent 系统从 0 到 1：**基于 LangGraph 设计意图分类、方案设计、骨架/组件生成、合并和最终校验工作流，以结构化 State 传递设计方案、几何约束与校验结果；结合 LangSmith Trace 和服务日志定位规划—执行失配。
- **RAG 知识治理与评测：**落地 rules-v3，限制普通生成只使用 `.wild` 协议、引擎能力和条件组装关系，通过 metadata、Loader 白名单、linter 和部署清单隔离建筑类型模板；使用 LangSmith Dataset / Experiment 管理 51 题评测集（含 35 题拒绝/隔离样本），结合 Hit@K、Recall@K、MRR 评估召回排序和错误注入。
- **Agent 可靠性闭环：**将校验结果结构化为逐实体 `ValidationIssue`，限制模型选择白名单局部修复动作，程序在 Blueprint 副本执行并全量复检；错误未减少或引入新问题时自动回滚，未通过门禁的蓝图禁止保存。
- **长任务与事件恢复：**将生成任务从 WebSocket 连接中解耦，持久化任务、Checkpoint 和事件序列，支持断线重连后的事件续传；通过版本化事件协议和动态心跳减少假断连与跨会话状态串扰。
- **CI/CD 与发布门禁：**使用 Jenkins Pipeline 与 Docker 串联构建、测试、镜像预检和生产发布，以知识清单和 `/health/ready` 阻断不完整镜像或未就绪 RAG 服务，新容器启动失败时自动恢复旧镜像。

## 9. 后续需要补充的证据

以下信息不是当前简历必须项，但可以进一步增强面试说服力：

- Agent 实际生成并成功导入 3D 世界的 Blueprint 数量；
- 一个完整的代表性建筑生成案例；
- `.wild` 与预制 GLB 在文件体积、加载内存或生产时间上的同条件对比；
- 工作流改造前后的成功率、失败类型、模型调用次数、Token 和耗时；
- rules-v3 在真实 Embedding 和固定索引下的完整检索报告；
- 纠错闭环对典型错误的发现率、修复成功率和平均重试次数；
- 断线恢复和部署回滚在真实环境中的故障记录。

没有这些数字不妨碍描述已实现的工程能力，但不能使用“提升 85%”“达到最优”“从小时级降到分钟级”等缺少定义和基线的结果。


## 10. 难点思路疏通

### 10.1 Agent工作流与中间状态设计问题

1. 问题：批准约束进入了 architecture 节点的 Prompt，
但没有进入候选归一化、评分和选择逻辑。